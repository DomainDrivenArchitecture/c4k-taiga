kind: Service
apiVersion: v1
metadata:
  name: taiga-events
  labels:
    app.kubernetes.part-of: c4k-taiga
    app.kubernetes.io/component: taiga-events
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: taiga
  ports:
    - name: http
      targetPort: http
      port: 8888
      protocol: TCP            
      